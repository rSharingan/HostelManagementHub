// path: src/features/dashboard/api.js
import axios from '../../lib/api/axios'

export const getDashboardStatsAPI = async () => {
  // Placeholder - implement based on backend
  return {
    totalStudents: 0,
    totalRooms: 0,
    occupancyRate: 0,
    pendingFees: 0,
  }
}
